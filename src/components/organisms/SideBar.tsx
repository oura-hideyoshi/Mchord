import React from 'react'

export const SideBar = () => {
    return (
        <div>
            <button onClick={() => console.clear()}>
                clear console
            </button>
        </div>
    )
}
